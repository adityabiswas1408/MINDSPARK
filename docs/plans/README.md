# Implementation Plans (`docs/plans/`)

This directory holds single-feature implementation plans for active features.

## Structure
- Each plan is titled `YYYY-MM-DD-<feature-slug>-plan.md` or placed in `docs/plans/<feature-slug>/PLAN.md`.
- Plans must define touched files, interfaces, and end-to-end verification checks.
