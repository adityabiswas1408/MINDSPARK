# Feature Specifications (`docs/specs/`)

This directory holds feature specifications and requirements documents for new feature work.

## Structure
- Each feature spec is titled `YYYY-MM-DD-<feature-slug>-spec.md` or placed in `docs/specs/<feature-slug>/SPEC.md`.
- Once a feature is shipped and verified, its spec is archived and closed out per the `project-context-system` lifecycle.
